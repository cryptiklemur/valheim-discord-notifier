on:
  - push

name: compile

jobs:
  build:
    name: Build project
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@main
      - uses: actions/setup-dotnet@v1
      - run: dotnet build DiscordNotifier.csproj